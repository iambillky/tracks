<!--
File: templates/base.html
Purpose: Base template with dark theme and sidebar navigation
Created: 2024-09-13
Author: iambilky

Revision History:
- v1.0.0: Initial creation with dark theme
- v1.0.1: Added sidebar navigation and quick stats
- v1.0.2: Integrated data centers link
- v1.1.0: Added PDU/Power management link
- v1.2.0: Added IPAM (IP Address Management) link
-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ title }}{% endblock %} - DCMS</title>
    
    <!-- Bootstrap CSS for base styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Custom CSS for dark theme -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #1a1f2e 0%, #151923 100%);
            color: #e4e6eb;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            min-height: 100vh;
            display: flex;
        }

        /* ========== SIDEBAR STYLES ========== */
        .sidebar {
            width: 320px;
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(10px);
            border-right: 1px solid rgba(255, 255, 255, 0.1);
            padding: 20px;
            display: flex;
            flex-direction: column;
            position: fixed;
            height: 100vh;
            overflow-y: auto;
        }

        .app-header {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 15px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 12px;
            margin-bottom: 10px;
        }

        .app-title {
            font-size: 1.3rem;
            font-weight: 600;
            color: #fff;
        }

        .app-date {
            font-size: 0.9rem;
            color: #8b92a5;
            padding: 0 15px;
            margin-bottom: 20px;
        }

        /* ========== NAVIGATION STYLES ========== */
        .nav-section {
            flex: 1;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 16px;
            margin: 4px 0;
            border-radius: 8px;
            color: #e4e6eb;
            text-decoration: none;
            transition: all 0.2s;
            font-size: 0.95rem;
        }

        .nav-item:hover {
            background: rgba(255, 255, 255, 0.05);
            color: #fff;
            transform: translateX(2px);
        }

        .nav-item.active {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.15), rgba(59, 130, 246, 0.1));
            border-left: 3px solid #3b82f6;
            color: #fff;
        }

        .nav-icon {
            width: 20px;
            text-align: center;
        }

        .nav-divider {
            height: 1px;
            background: rgba(255, 255, 255, 0.1);
            margin: 15px 0;
        }

        /* ========== MAIN CONTENT STYLES ========== */
        .main-content {
            margin-left: 320px;
            flex: 1;
            padding: 30px;
            overflow-y: auto;
        }

        /* ========== CARD STYLES ========== */
        .card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 20px;
            backdrop-filter: blur(10px);
        }

        .card-title {
            color: #fff;
            font-size: 1.25rem;
            margin-bottom: 15px;
        }

        /* ========== TABLE STYLES ========== */
        .table-dark-custom {
            background: transparent;
            color: #e4e6eb;
        }

        .table-dark-custom th {
            background: rgba(255, 255, 255, 0.05);
            color: #fff;
            border-color: rgba(255, 255, 255, 0.1);
            font-weight: 600;
        }

        .table-dark-custom td {
            border-color: rgba(255, 255, 255, 0.1);
        }

        .table-dark-custom tbody tr:hover {
            background: rgba(255, 255, 255, 0.02);
        }

        /* ========== BUTTON STYLES ========== */
        .btn-primary {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
            border: none;
            padding: 8px 20px;
            transition: all 0.2s;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, #2563eb, #1d4ed8);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
        }

        .btn-success {
            background: linear-gradient(135deg, #10b981, #059669);
            border: none;
        }

        .btn-success:hover {
            background: linear-gradient(135deg, #059669, #047857);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
        }

        .btn-danger {
            background: linear-gradient(135deg, #ef4444, #dc2626);
            border: none;
        }

        .btn-danger:hover {
            background: linear-gradient(135deg, #dc2626, #b91c1c);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
        }

        /* ========== ALERT STYLES ========== */
        .alert {
            border: none;
            border-radius: 8px;
            padding: 12px 20px;
        }

        .alert-success {
            background: rgba(16, 185, 129, 0.1);
            color: #10b981;
            border-left: 3px solid #10b981;
        }

        .alert-danger {
            background: rgba(239, 68, 68, 0.1);
            color: #ef4444;
            border-left: 3px solid #ef4444;
        }

        .alert-warning {
            background: rgba(245, 158, 11, 0.1);
            color: #f59e0b;
            border-left: 3px solid #f59e0b;
        }

        /* ========== FORM STYLES ========== */
        .form-control, .form-select {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: #e4e6eb;
            transition: all 0.2s;
        }

        .form-control:focus, .form-select:focus {
            background: rgba(255, 255, 255, 0.08);
            border-color: #3b82f6;
            color: #fff;
            box-shadow: 0 0 0 0.25rem rgba(59, 130, 246, 0.25);
        }

        .form-label {
            color: #e4e6eb;
            font-weight: 500;
            margin-bottom: 5px;
        }

        /* ========== PAGE HEADER ========== */
        .page-header {
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .page-title {
            font-size: 2rem;
            font-weight: 700;
            color: #fff;
            margin-bottom: 5px;
        }

        .page-subtitle {
            color: #8b92a5;
            font-size: 1rem;
        }

        /* ========== QUICK STATS ========== */
        .quick-stats {
            margin-top: 30px;
            padding: 15px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 8px;
        }

        .quick-stats-title {
            font-size: 0.85rem;
            color: #8b92a5;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 10px;
        }

        .stat-item {
            display: flex;
            justify-content: space-between;
            padding: 5px 0;
            font-size: 0.9rem;
        }

        .stat-label {
            color: #8b92a5;
        }

        .stat-value {
            color: #fff;
            font-weight: 600;
        }
    </style>
    
    {% block extra_css %}{% endblock %}
</head>
<body>
    <!-- ========== SIDEBAR ========== -->
    <div class="sidebar">
        <div class="app-header">
            <span style="font-size: 1.5rem;">🖥️</span>
            <div class="app-title">DCMS</div>
        </div>
        
        <div class="app-date">
            {{ current_date or 'September 14, 2025' }}
        </div>
        
        <nav class="nav-section">
            <a href="{{ url_for('index') }}" class="nav-item {% if request.endpoint == 'index' %}active{% endif %}">
                <span class="nav-icon">🏠</span>
                <span>Dashboard</span>
            </a>
            
            <div class="nav-divider"></div>
            
            <a href="{{ url_for('datacenters.index') }}" class="nav-item {% if 'datacenter' in request.endpoint %}active{% endif %}">
                <span class="nav-icon">🏢</span>
                <span>Data Centers</span>
            </a>
            
            <a href="{{ url_for('network_devices.index') }}" class="nav-item {% if 'network_device' in request.endpoint %}active{% endif %}">
                <span class="nav-icon">🔌</span>
                <span>Network Devices</span>
            </a>
            
            <a href="{{ url_for('pdus.index') }}" class="nav-item {% if 'pdu' in request.endpoint %}active{% endif %}">
                <span class="nav-icon">⚡</span>
                <span>PDUs / Power</span>
            </a>
            
            <a href="{{ url_for('ipam.index') }}" class="nav-item {% if 'ipam' in request.endpoint %}active{% endif %}">
                <span class="nav-icon">🌐</span>
                <span>IP Management</span>
            </a>
            
            <div class="nav-divider"></div>
            
            <a href="#" class="nav-item">
                <span class="nav-icon">🖥️</span>
                <span>Servers</span>
                <span style="margin-left: auto; font-size: 0.75rem; color: #8b92a5;">Coming Soon</span>
            </a>
            
            <a href="#" class="nav-item">
                <span class="nav-icon">💾</span>
                <span>Storage</span>
                <span style="margin-left: auto; font-size: 0.75rem; color: #8b92a5;">Coming Soon</span>
            </a>
            
            <a href="#" class="nav-item">
                <span class="nav-icon">📊</span>
                <span>Reports</span>
                <span style="margin-left: auto; font-size: 0.75rem; color: #8b92a5;">Coming Soon</span>
            </a>
            
            <div class="nav-divider"></div>
            
            <a href="{{ url_for('about') }}" class="nav-item {% if request.endpoint == 'about' %}active{% endif %}">
                <span class="nav-icon">ℹ️</span>
                <span>About</span>
            </a>
        </nav>
        
        <!-- Quick Stats Section -->
        {% if dc_count is defined %}
        <div class="quick-stats">
            <div class="quick-stats-title">Quick Stats</div>
            <div class="stat-item">
                <span class="stat-label">Data Centers:</span>
                <span class="stat-value">{{ dc_count or 0 }}</span>
            </div>
            <div class="stat-item">
                <span class="stat-label">Total Racks:</span>
                <span class="stat-value">{{ rack_count or 0 }}</span>
            </div>
            <div class="stat-item">
                <span class="stat-label">Network Devices:</span>
                <span class="stat-value">{{ network_device_count or 0 }}</span>
            </div>
            <div class="stat-item">
                <span class="stat-label">PDUs:</span>
                <span class="stat-value">{{ pdu_count or 0 }}</span>
            </div>
            {% if total_ips is defined %}
            <div class="stat-item">
                <span class="stat-label">IPs Tracked:</span>
                <span class="stat-value">{{ total_ips or 0 }}</span>
            </div>
            {% endif %}
        </div>
        {% endif %}
    </div>
    
    <!-- ========== MAIN CONTENT ========== -->
    <div class="main-content">
        <!-- Flash Messages -->
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="alert alert-{{ 'danger' if category == 'error' else category }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}
        
        <!-- Page Content -->
        {% block content %}{% endblock %}
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Custom JS -->
    {% block extra_js %}{% endblock %}
</body>
</html>